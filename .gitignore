# loom.lock is committed on purpose — it is the reproducibility pin (ADR-0002),
# generated by `build`/`update`, never hand-edited.
.env
.env.*
!.env.example
.claude/settings.local.json
.claude/memory/
.scratch/
*.log
.DS_Store

# built engine binary (bootstrap output)
/bin/

# per-project runtime state: action log + materialized $HOME staging
.loom/
